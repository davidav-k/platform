server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
        - id: notification-service-root
          uri: lb://notification-service
          predicates:
            - Path=/api/notifications,/api/notifications/
          filters:
            - RewritePath=/api/notifications/?$, /api/v1/notifications

        - id: notification-service
          uri: lb://notification-service
          predicates:
            - Path=/api/notifications/**
          filters:
            - RewritePath=/api/notifications/(?<segment>.*), /api/v1/notifications/${segment}

        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/users/**
            - Method=POST,GET,PUT,PATCH,DELETE,OPTIONS
          filters:
            - RewritePath=/api/users/(?<segment>.*), /api/v1/user/${segment}
            - name: CircuitBreaker
              args:
                name: userService
                fallbackUri: forward:/fallback/user-service

        - id: task-service-root
          uri: lb://task-service
          predicates:
            - Path=/api/tasks,/api/tasks/
            - Method=POST,GET,OPTIONS
          filters:
            - SetPath=/api/v1/tasks
            - name: CircuitBreaker
              args:
                name: taskService
                fallbackUri: forward:/fallback/task-service

        - id: task-service
          uri: lb://task-service
          predicates:
            - Path=/api/tasks/**
            - Method=POST,GET,PUT,PATCH,DELETE,OPTIONS
          filters:
            - RewritePath=/api/tasks/(?<segment>.*), /api/v1/tasks/${segment}
            - name: CircuitBreaker
              args:
                name: taskService
                fallbackUri: forward:/fallback/task-service

      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin

      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka # for Docker
#       defaultZone: http://localhost:8761/eureka  # for Idea
management:
  endpoints:
    web:
      exposure:
        include: health,info

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 5s
        failureRateThreshold: 50
        eventConsumerBufferSize: 10
    instances:
      userService:
        baseConfig: default
      taskService:
        baseConfig: default


logging:
  level:
    org.springframework.cloud.gateway: DEBUG
