# Kubernetes Infrastructure

## Status

Planned only. This directory does not currently contain Kubernetes manifests,
Helm charts, or a deployable Kubernetes configuration.

Use the root Docker Compose workflow for local MVP development. Kubernetes and
Helm deployment instructions should be added only after deployable resources
exist.
