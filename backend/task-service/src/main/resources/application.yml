spring:
  application:
    name: task-service

outbox:
  publisher:
    enabled: false
    adapter: logging
    batch-size: 20
    max-retries: 3
    fixed-delay: 5s
    kafka:
      enabled: false
      bootstrap-servers: kafka:9092
      topic: platform.task-events
